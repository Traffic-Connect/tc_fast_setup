# 🔄 Инструкция по обновлению на сервере

## Проблемы, которые нужно исправить:

1. **HestiaCP пользователь уже существует** - исправлено
2. **Функции не найдены** - исправлено
3. **Путь к политике безопасности** - исправлено

## Команды для выполнения на сервере:

```bash
# 1. Перейти в папку проекта
cd ~/tc_fast_setup

# 2. Обновить репозиторий
git pull origin main

# 3. Проверить, что изменения применились
git log --oneline -3

# 4. Запустить установку заново
./install.sh
```

## Что было исправлено:

### ✅ HestiaCP пользователь
- Добавлено уникальное имя пользователя с временной меткой
- Автоматическое удаление конфликтующего пользователя
- Автоматическое определение hostname

### ✅ Функции установки
- `install_monitoring_system` → `install_monitoring`
- `setup_web_templates` → `install_templates`

### ✅ Пути к файлам
- Исправлен путь к политике безопасности
- Добавлены недостающие переменные

## Ожидаемый результат:

После обновления установка должна пройти без ошибок:
- ✅ HestiaCP установится с уникальным пользователем
- ✅ Система мониторинга установится корректно
- ✅ Шаблоны веб-сервера настроятся
- ✅ Политика безопасности загрузится

## Если проблемы останутся:

```bash
# Проверить статус git
git status

# Принудительно обновить
git fetch origin
git reset --hard origin/main

# Проверить синтаксис скриптов
bash -n install.sh
bash -n core/installers/main_install.sh
bash -n system/admin/admin_install.sh
bash -n system/monitoring/monitoring_install.sh
bash -n web/templates/templates_install.sh
```
